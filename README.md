# Domain-Adaptive-Embeddings
Cross-domain feature alignment with triplet loss on Fashion-MNIST that simulates DRR to X Ray modality transfer using a CNN embedding network, PCA projection, and Top-1 retrieval validation.
